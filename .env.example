# ============================================
# AI Art Generator - Environment Configuration
# ============================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# ============================================
# AI Provider Configuration
# ============================================
# Select which AI provider to use for both prompt enhancement and image generation
# Options: 'openai' or 'gemini'
# Default: 'openai'
# Note: The same provider is used for both LLM (prompt enhancement) and image generation
# Gemini image generation uses LangChain ChatGoogleGenerativeAI with gemini-2.5-flash-image model
# Gemini image generation is now fully supported using Gemini 2.5 Flash Image model
PROVIDER=openai

# ============================================
# OpenAI Configuration
# ============================================
# Required if PROVIDER=openai
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI model for prompt enhancement
# Options: 'gpt-3.5-turbo', 'gpt-4', 'gpt-4-turbo', etc.
# Default: 'gpt-3.5-turbo'
OPENAI_MODEL=gpt-3.5-turbo

# Optional: Temperature for OpenAI model (0.0 to 2.0)
# Higher values make output more random, lower values more deterministic
# Default: 0.7
OPENAI_TEMPERATURE=0.7

# ============================================
# Google Gemini Configuration
# ============================================
# Required if PROVIDER=gemini
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Gemini model (used for both prompt enhancement and image generation)
# For image generation, use models like 'gemini-2.5-flash-image'
# For text tasks, use models like 'gemini-pro', 'gemini-2.5-flash'
# Options: 'gemini-pro', 'gemini-pro-vision', etc.
# Default: 'gemini-pro'
GEMINI_MODEL=gemini-pro
# Note: This model is used for both prompt enhancement (LLM) and image generation.
# For image generation, models like 'gemini-2.5-flash-image' work best.
# For text tasks, use 'gemini-pro' or 'gemini-2.5-flash'.
# If you set an image model here, it will be used for both tasks.
